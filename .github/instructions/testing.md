# Testing Instructions

- All business logic in `app/_services/` must have tests.
- Critical utilities in `app/_lib/` must have tests.
- Use Jest for unit tests and Playwright for e2e tests.
- Test:
  - Happy paths
  - Edge cases
  - Error cases

Tests should be placed in:

- tests/
